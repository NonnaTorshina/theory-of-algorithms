services:
  sleep-tracker:
    build: .
    container_name: sleep-tracker-app
    volumes:
      - ./sleep_tracker.db:/app/sleep_tracker.db
      - ./data:/app/data
    ports:
      - "8000:8000"
    environment:
      PYTHONUNBUFFERED: 1