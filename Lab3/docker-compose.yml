version: '3.8'

services:
  sleep-tracker:
    build: .
    ports:
      - "8000:8000"  # или другой порт, который использует ваше приложение
    volumes:
      - sleep_tracker_data:/data
    environment:
      - DATABASE_PATH=/data/sleep_tracker.db

volumes:
  sleep_tracker_data: